import React from 'react'

const CartProductsCard = ({data}) => {
    const { id, title, price, image, rating, category, index } = data;
    
  return (
    <div>CartProductsCard</div>
  )
}

export default CartProductsCard